const array = [10, 20, 30, 40, 50, 60];
if (array.indexOf(20) >= 0 && array.indexOf(70) >= 0) {
    console.log("The element to check is in the array");
} else {
    console.log("The element to check is NOT in the array");
    
}