const isLong = (string) => {
    // instructions de la fonction
    if(string.length >= 6)
    {
        console.log("It's a long string.");
    }
    else{
        console.log("It's not a long string.");
    }
};

isLong("JavaScript");