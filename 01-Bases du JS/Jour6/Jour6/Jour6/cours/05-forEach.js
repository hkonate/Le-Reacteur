// ForEach parcourt le tableau qu'on lui donne et exécute la callback avec comme argument chacun des éléments du tableau

const array1 = ["c", "a", "b"];

// const display = (arg) => {
//   console.log(arg);
// };

// array1.forEach(display);

array1.forEach((arg) => {
  console.log(arg);
});
