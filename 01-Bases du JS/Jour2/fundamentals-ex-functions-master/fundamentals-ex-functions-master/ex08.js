// Début de votre code
const showAddress = (adress, zip_code, city) => {
    console.log(adress);
    console.log(zip_code);
    console.log(city);
}
// Fin de votre code

showAddress("9 rue du Château d'Eau", "75010", "Paris"); // Doit afficher la rue, le code postal et la ville sur 3 lignes différentes dans le terminal
